# Core dependencies for the writing analysis tools
# No external libraries required for basic functionality

# Optional dependencies for enhanced features
matplotlib>=3.5.0  # For visualization features
plotly>=5.0.0      # For interactive charts
pandas>=1.3.0      # For data manipulation
numpy>=1.21.0      # For numerical computations

# For JSON and CSV output (built into Python)
# For text processing (built into Python)

# Web server capabilities (if building MCP servers)
fastapi>=0.68.0
uvicorn>=0.15.0

# For development and testing
pytest>=6.2.0
pytest-cov>=2.12.0
